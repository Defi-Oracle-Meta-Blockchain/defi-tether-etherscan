# Application Development Properties

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/defi_db
spring.datasource.username=root
spring.datasource.password=secret
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

# Logging Configuration
logging.level.root=INFO
logging.level.com.defi=DEBUG
logging.file.name=logs/application-dev.log

# Web3j Configuration
web3j.client-address=http://localhost:8545

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin

# Custom Application Properties
app.name=DeFi Application
app.description=Development environment for DeFi application
app.version=1.0.0

# Email Configuration
spring.mail.host=smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=your_mailtrap_username
spring.mail.password=your_mailtrap_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Cache Configuration
spring.cache.type=simple

# Additional Properties
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoint.metrics.enabled=true

# Test Configuration
spring.test.database.replace=ANY
spring.datasource.initialization-mode=always

# Integration Test Configuration
spring.integration.test.enabled=true

# Mock Configuration
mock.web3j.enabled=true
mock.credentials.enabled=true

# Blockchain Configuration
blockchain.network=mainnet
blockchain.gas-provider=default

# API Configuration
api.rate-limit=1000
api.timeout=5000

# Feature Toggles
feature.toggles.enable-new-feature=true

# Monitoring and Metrics
management.metrics.export.prometheus.enabled=true
management.metrics.export.prometheus.rsocket.enabled=false
management.metrics.export.prometheus.pushgateway.enabled=false

# End Generation Here
